[project]
name = "cryowire-spec"
version = "0.1.0"
description = "Specification for dilution refrigerator wiring configuration data"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
docs = [
    "zensical",
]
