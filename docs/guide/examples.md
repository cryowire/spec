# Examples

Complete sample files are available in the [`examples/`](https://github.com/cryo-wiring/spec/tree/main/examples) directory.

## Files

| File | Description |
|------|-------------|
| `components.yaml` | Component catalog |
| `metadata.yaml` | Cooldown metadata |
| `control.yaml` | Control wiring (module format) |
| `readout_send.yaml` | Readout send wiring (module format) |
| `readout_return.yaml` | Readout return wiring (flat format) |

## Component Catalog

```yaml
--8<-- "examples/components.yaml"
```

## Cooldown Metadata

```yaml
--8<-- "examples/metadata.yaml"
```

## Control Lines (Module Format)

```yaml
--8<-- "examples/control.yaml"
```

## Readout Send Lines (Module Format)

```yaml
--8<-- "examples/readout_send.yaml"
```

## Readout Return Lines (Flat Format)

```yaml
--8<-- "examples/readout_return.yaml"
```
